This is a simple programming assignment to show the performance differences between differnet scheduling algorithms.

Running it:
In order to run this program simply compile the java files then run the A1 class with the arguments "datafile1.txt". The results will be displayed in the console.
The text file contains info on the processes and what time they arrive in the queue, changing this obviously changes the results. 
